# Optimization

Quadratic and non-convex cost functions, linear constraints and bounds, wrappers around `scipy.optimize.minimize` and `minimize_scalar`, and loss-surface evaluation for plotting.

::: utils.optimization_utils
    options:
      docstring_style: numpy
      show_root_heading: false
      members_order: source
      filters: ["!^_"]
